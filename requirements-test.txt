# Test + eval dependencies. Kept separate from runtime requirements so
# Lambda packages don't pick them up.

# Lambda runtime deps still need to import for tests
flask>=3.0.0
boto3>=1.34.0

# Test framework
pytest>=8.0.0,<9
pytest-mock>=3.12.0,<4

# Schema validation in unit tests + eval harness
jsonschema>=4.20.0,<5

# Lint
ruff>=0.5.0,<1.0
